{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "backend-chart.serviceAccountName" . }}
  labels:
{{ include "backend-chart.labels" . | indent 4 }}
{{- end -}}
